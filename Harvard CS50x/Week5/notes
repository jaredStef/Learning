Linked List

[int]
[pointer to next int]

first->1->2->4->NULL

upside is the ability to grow
price is twice as much memory used

give up random access goes from O(1) to O(n)

typedef struct node
{
  int n;
  struct node* next;
} node;

binary tree has 2 sub-nodes max

get a number from user
int x;
// match %i and store in address of x
scanf("%i", &x);

typedef struct node
{
  char c;
  trie* next;
};

typedef struct trie
{
  node letters[26];
} trie;

// hash function
int name(char letter)
{
  return letter.toLowerCase() - 'a';
}
